import styled from '@emotion/styled';

const SideBarItemDropdownWrapper = styled.div({
  position: 'absolute',
  width: '300px',
  marginLeft: '-10px',
  border: '1px solid #eff1f3',
  background: 'white',
  boxShadow: '0px 0px 20px 5px #d2d2d2',
});

export default SideBarItemDropdownWrapper;
